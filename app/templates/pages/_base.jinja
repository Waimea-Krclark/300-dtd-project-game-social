{#=====================================================
  Base template
  - Used as the basis of all pages
=====================================================#}

<!DOCTYPE html>

<html lang="en" data-theme="light">

<head>
	<meta charset="utf-8">
	<meta name="viewport" content="width=device-width, initial-scale=1">

	<title>Checkpoint {% block title %}{% endblock %}</title>

    <link rel="icon" href="/static/images/note.svg">

    <script src="/static/js/utils.js"></script>
    <script src="/static/js/switch.js"></script>
    <script src="/static/js/navOption.js"></script>

    <link rel="stylesheet" href="/static/css/reset.css">
    <link rel="stylesheet" href="/static/css/messages.css">
    <link rel="stylesheet" href="/static/css/styles.css">
</head>

<body>

    <header>
        <a href="/">
            Checkpoint
        </a>

        <nav>
            <a href="/">Home</a>
            <a href="/">Back</a>
            <a href="javascript:void(0);" onclick="toggleOptionMenu()">Profile</a>
            {% if session.logged_in %} 
                <section class="menu" id="menuDrop">
                    <p>Welcome: {{session.user.username}}</p>
                    <a href="/user/profile/{{session.user.user_id}}">Profile</a>
                    <a href="/logout">Logout</a>
                </section>
            {% else %}
                <section class="menu" id="menuDrop">
                    <a href="/user/login">Sign In</a>
                    <a href="/user/signup">Sign Up</a>
                </section>
            {% endif %}
            
        </nav>
    </header>

    {# Show flash messages from any previous events #}
    {% include "partials/messages.jinja" %}

    <main>
        <section id="page_content">
            {% block content %}
            {% endblock %}
        </section>
    </main>

</body>

</html>

